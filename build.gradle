allprojects {
    group 'cm-platform'
    version '0.0.1-SNAPSHOT'

    apply plugin: 'java'
    apply plugin: 'war'

    repositories {
        //使用开源中国的maven仓库
        maven { url 'http://mvnrepository.com/' }
    }
}

dependencies {
    compile project(':cm-web-war')
}
